name: iOS Build

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build and Archive
    runs-on: macos-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate

      - name: Build for Physical iPad (Unsigned)
        run: |
          set -o pipefail
          xcodebuild clean build \
            -project LyricSlicerAI.xcodeproj \
            -scheme LyricSlicerApp \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | xcpretty

      - name: Package .IPA for Sideloading
        run: |
          mkdir -p Payload
          # Find the built .app in the derived data or build directory
          APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData -name "LyricSlicerApp.app" -type d | head -n 1)
          if [ -d "$APP_PATH" ]; then
            cp -r "$APP_PATH" Payload/
            zip -qr LyricSlicerAI.ipa Payload
          else
            echo "App bundle not found"
            exit 1
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: LyricSlicerAI-iPad-Build
          path: LyricSlicerAI.ipa
